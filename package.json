{
  "name": "diff-json",
  "version": "1.0.0",
  "description": "Read two json files (presumably from my prometheus focused mdns query service). show diff focused on IP or host name.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Willy",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "json2csv": "^6.0.0-alpha.2",
    "lodash": "^4.17.21"
  }
}
